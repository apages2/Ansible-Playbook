---
  - name: Build Router Templates
    hosts: SWITCHS
    connection: local
    gather_facts: no

    tasks:
      - name: Build configs
        template:
          src=templates/switchs.j2
          dest=/home/administrator/configs/{{inventory_hostname}}.conf
